apiVersion: v1
kind: Service
metadata:
  name: go-k8s-service
  labels:
    app: go-k8s
spec:
  type: NodePort
  selector:
    app: go-k8s
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
